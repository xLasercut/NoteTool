const path = require('path');
const fs = require('fs');
const q = require('q');
const remote = require('electron').remote;
const BrowserWindow = remote.BrowserWindow;
const url = require('url');
const windowHelper = require('../script/window-helper.js');
const fileHelper = require('../script/file-helper.js');
const DEBUG = false;
